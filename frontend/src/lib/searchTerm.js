// ล้างคำค้นก่อนส่งเข้า .or()/.ilike() ของ supabase-js
//
// ตัวคั่นของ .or() เป็นสตริง filter ดิบ ถ้าปล่อยให้ผู้ใช้พิมพ์ "," หรือ ")"
// ผ่านเข้าไปได้ จะต่อเงื่อนไขใหม่ที่ไม่ตั้งใจเข้าไปในคำสั่งได้ (เช่นแอบเทียบ
// คอลัมน์อื่นที่ไม่ได้เปิดให้ค้น) ส่วน % กับ _ เป็น wildcard ของ ilike
//
// ทุกหน้าที่มีช่องค้นหาต้อง import ตัวนี้ ห้ามเขียนกฎตัดอักขระเองซ้ำ
export const escapeSearchTerm = (value) => value.replace(/[%_,()]/g, "").trim();
